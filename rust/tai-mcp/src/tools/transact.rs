//! Spending tools (Task 5). Stub only — no tools registered yet.
//! Full implementation in Task 5.
use crate::protocol::Tool;
use crate::tools::Ctx;

pub fn register(_ctx: &Ctx, _out: &mut Vec<Box<dyn Tool>>) {
    // Task 5: tai_buy, tai_sell, tai_pay, tai_hire, tai_treasury_topup,
    // tai_treasury_withdraw will be registered here.
}
